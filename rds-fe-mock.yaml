apiVersion: v1
kind: Service
metadata:
  name: rds-fe-mock
  namespace: demo-rds
  labels:
    app: rds-fe
    app.kubernetes.io/component: rds-fe
    app.kubernetes.io/instance: rds-fe
spec:
  selector:
    deployment: rds-fe
  ports:
    - name: 3000-tcp
      protocol: TCP
      port: 3000
      targetPort: 3000
